import 'package:flutter/material.dart';

class ChatInitialList extends StatelessWidget {
  const ChatInitialList({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('チャットをにゅりょくしてください'));
  }
}
